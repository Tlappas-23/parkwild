# secrets
.env

# virtualenv + python noise
.venv/
__pycache__/
*.pyc
.pytest_cache/
.ipynb_checkpoints/
*.egg-info/
build/

# data: images, DuckDB, model output. Regenerable from the scripts.
data/*
!data/.gitkeep
# ...except the human-entered review verdicts, which are small and irreplaceable.
# (git can't re-include a file under an ignored directory, hence the two-step dance)
!data/review/
data/review/*/*
!data/review/*/review.csv

# macOS
.DS_Store

# run logs are noise; the decision log is a record and stays tracked
reports/*.log

# raw model downloads are re-derivable from the sources in config/models.toml; the compressed ones ship
app/tsconfig.tsbuildinfo
