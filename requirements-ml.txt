# Heavy dependencies. Installs PyTorch (Apple-silicon wheels use the MPS backend),
# yolov5 and SpeciesNet. First run of SpeciesNet also downloads ~1 GB of weights
# from Kaggle Hub / Hugging Face into your cache. Install with `make setup-ml`
# only after deciding you want that on this machine.
speciesnet>=5.0
